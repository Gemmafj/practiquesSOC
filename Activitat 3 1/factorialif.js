var a = parseInt(prompt("Quin factorial vols calcular?"));
            var total = 1;

            var b;
        for (var i = 1; i <= a; i ++) {
            total = total*i;
            document.write(total+"<br>");
        }
